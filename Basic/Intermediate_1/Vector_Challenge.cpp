// Vector manipulation challenge (چالش دستکاری وکتورها)


// Input/output library (کتابخانه ورودی/خروجی)
 
// Required for vectors (ضروری برای استفاده از وکتورها)
 
// Avoids "std::" prefix (جلوگیری از پیشوند "std::")

int main() {

    // Create two empty integer vectors (ایجاد دو وکتور صحیح خالی)


    // Add elements to vector1 (افزودن عناصر به vector1)


    // Display vector1 contents (نمایش محتوای vector1)


    // Add elements to vector2 (افزودن عناصر به vector2)


    // Display vector2 contents (نمایش محتوای vector2)


    // Create 2D vector (ایجاد وکتور دو‌بعدی)


    // Add vectors to 2D structure (افزودن وکتورها به ساختار دو‌بعدی)


    // Display 2D vector contents (نمایش محتوای وکتور دو‌بعدی)


    // Modify original vector (تغییر وکتور اصلی)


    // Display 2D vector again (دوباره نمایش وکتور دو‌بعدی)


    // Display modified vector1 (نمایش vector1 تغییر‌یافته)

    return 0;

}

/*
Challenge Purpose (هدف چالش):
Test understanding of vector behavior
(آزمون درک رفتار وکتورها).

Explore how vectors interact in nested structures 
(کاوش تعامل وکتورها در ساختارهای تو در تو).

Key Learning Points (نکات یادگیری کلیدی):
Vector copying: When added to 2D vector, copies are made 
(کپی‌برداری وکتورها: هنگام افزودن به وکتور دو‌بعدی، کپی‌ها ساخته می‌شوند).

Independence: Changes to original don't affect copies 
(استقلال: تغییرات در اصل، کپی‌ها را تحت تأثیر نمی‌گذارد).

Memory management: Vectors handle their own memory 
(مدیریت حافظه: وکتورها حافظه خود را مدیریت می‌کنند).
*/