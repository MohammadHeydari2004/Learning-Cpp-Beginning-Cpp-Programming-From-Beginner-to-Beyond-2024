#include <iostream> // Input/output stream library (کتابخانه جریان ورودی/خروجی)

int main() {

    int favorite_number; // Declare integer variable (تعریف متغیر صحیح)

    std::cout << "Hello world" << std::endl; // Print message (چاپ پیام)

    return 0; // Program exit status (وضعیت خروج برنامه)

}

/*
int favorite_number;: Declares an unused integer variable 
(تعریف یک متغیر صحیح بدون استفاده).

Shows that variables can exist without immediate use
(نشان می‌دهد متغیرها می‌توانند بدون استفاده تعریف شوند).
*/