#include <iostream> // Input/output stream library (کتابخانه جریان ورودی/خروجی)

int main() {

 //   std::cout << ("Hello world" / 125) << std::endl; // Invalid operation (عملیات نامعتبر)

    return 0; // This line won't compile (این خط کامپایل نمی‌شود)

}

/*
"Hello world" / 125: Invalid string division
(تقسیم رشته نامعتبر است).

Strings cannot be divided by numbers
(رشته‌ها نمی‌توانند بر اعداد تقسیم شوند).

Causes a compilation error
(خطای کامپایل ایجاد می‌کند).
*/