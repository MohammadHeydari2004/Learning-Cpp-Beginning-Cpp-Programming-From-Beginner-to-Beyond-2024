/*
	Section 9 Challenge (بخش 9: چالش آموزشی)

	This program creates a collection of integers and allows the user to:
	- Print numbers
	- Add a number
	- Display mean of numbers
	- Display smallest number
	- Display largest number
	- Quit

	این برنامه یک مجموعه از اعداد صحیح ایجاد می‌کند و به کاربر اجازه می‌دهد:
	- چاپ اعداد
	- افزودن عدد
	- نمایش میانگین اعداد
	- نمایش کوچک‌ترین عدد
	- نمایش بزرگ‌ترین عدد
	- خروج

	Features:
	- Uses a vector to store numbers (استفاده از وکتور برای ذخیره اعداد)
	- Provides a menu-driven interface (رابط کاربری مبتنی بر منو)
	- Handles empty list cases (مدیریت موارد لیست خالی)

	ویژگی‌ها:
	- استفاده از وکتور برای ذخیره اعداد
	- رابط کاربری مبتنی بر منو
	- مدیریت موارد لیست خالی
*/
#include <iostream> // Required for I/O (ضروری برای ورودی/خروجی)

int main() {
	std::cout << "Hello world" << std::endl;

	// TODO: Implement the following features (پیاده‌سازی ویژگی‌های زیر):
	// 1. Create a vector to store integers (ایجاد یک وکتور برای ذخیره اعداد صحیح)
	// 2. Display a menu with options (نمایش منو با گزینه‌ها)
	// 3. Process user input (پردازش ورودی کاربر)
	// 4. Implement 'P' option to print numbers (پیاده‌سازی گزینه 'P' برای چاپ اعداد)
	// 5. Implement 'A' option to add a number (پیاده‌سازی گزینه 'A' برای افزودن عدد)
	// 6. Implement 'M' option to calculate mean (پیاده‌سازی گزینه 'M' برای محاسبه میانگین)
	// 7. Implement 'S' and 'L' options for min/max (پیاده‌سازی گزینه‌های 'S' و 'L' برای کمینه/بیشینه)
	// 8. Implement 'Q' option to quit (پیاده‌سازی گزینه 'Q' برای خروج)

	// BONUS CHALLENGES (چالش‌های اضافی):
	// - Search for a number and display its frequency (جستجو برای یک عدد و نمایش فراوانی آن)
	// - Add option to clear the list (افزودن گزینه برای پاک کردن لیست)
	// - Prevent duplicate entries (جلوگیری از ورودی‌های تکراری)

	return 0;
}

/*
هدف: چالش تعاملی برای تمرین همه مفاهیم یادگرفته‌شده.

نکته آموزشی:

این یک تمرین تعاملی است تا شما خودتان راه‌حل را کشف کنید.
برای پیاده‌سازی از مفاهیم زیر استفاده کنید:
وکتورها برای ذخیره داده‌ها
حلقه do-while برای منوی کاربری
دستورات شرطی برای پردازش گزینه‌ها
حلقه‌ها برای پیمایش وکتور
قبل از دیدن راه‌حل، حتماً خودتان تلاش کنید!
*/